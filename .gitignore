# Session names are written here at runtime (g:claude_session_store).
data/

# Local Claude Code configuration, including the opt-in session-panel
# status hook (see examples/hooks/claude-vim-status.mjs).
.claude/settings.local.json
